import random

from functions import calculateFolding

def hillclimb (Protein, tries):

    aminoCoordinates = [(0,0),(0,1)]
    bestScore = 0
    bestFolding = []

    stuck = 0
    loops = 0

    while success != tries:
        amino = 1

        while amino < len(Protein.proteinChain):



        if stuck == 0
            succes += 1
        loops += 1

    return (bestScore, bestFolding)
