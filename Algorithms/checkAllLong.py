import copy
from functions import calculateFolding
from functions import calculateFolding

# PseudoCode:

# pakt het eiwit en zet het in groepen van 8

def checkAllLong(Protein):

    proteinChain = Protein.proteinChain

    fragmants = []
    nextt = 0

    Length = len(proteinChain)
    while nextt < Length:
        add = []
        for aminozuur in range(8):
            add.append(poteinChain[aminozuur])
            nextt +=1
                if nextt == Length:
                    pass
        fragments.appnd(add)
    beans = []

    for bean in beans:
